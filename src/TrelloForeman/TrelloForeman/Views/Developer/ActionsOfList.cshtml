@using Jil
@{
    ViewBag.Title = "Actions of List - Developer";
}

<h2>Cards</h2>

<pre>
@JSON.Serialize(ViewBag.Actions, new Options(dateFormat: DateTimeFormat.ISO8601, serializationNameFormat: SerializationNameFormat.CamelCase, prettyPrint: true))
</pre>