@using Jil
@{
    ViewBag.Title = "Cards - Developer";
}

<h2>Cards</h2>

<pre>
@JSON.Serialize(ViewBag.Cards, new Options(dateFormat: DateTimeFormat.ISO8601, serializationNameFormat: SerializationNameFormat.CamelCase, prettyPrint: true))
</pre>